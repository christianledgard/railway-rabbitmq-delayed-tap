# AMQP listener: bind on IPv4 and IPv6 explicitly for proxies/load balancers.
listeners.tcp.1 = 0.0.0.0:5672
listeners.tcp.2 = :::5672

# Management UI: bind on IPv4 so platform proxies can reach it reliably.
management.tcp.ip = 0.0.0.0
management.tcp.port = 15672
